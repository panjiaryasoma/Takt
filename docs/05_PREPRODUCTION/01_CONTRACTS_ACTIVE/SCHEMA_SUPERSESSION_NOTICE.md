# Schema Supersession Notice

`FEATURE_SCHEMA_FINAL.yaml` supersedes `3_EVALUATION_AND_DOMAIN_RULES/feature_schema_v1.0.yaml` for implementation contracts.

The older file remains as historical reasoning and traceability evidence. It should not be deleted until implementation uses the final schema and regression tests cover it.
