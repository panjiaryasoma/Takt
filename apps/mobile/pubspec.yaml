name: takt_mobile
description: Calendar-aware competition decision support mobile client.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  drift: ^2.35.0
  drift_flutter: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ">=2.15.1 <2.16.0"
  drift_dev: ^2.35.0

flutter:
  uses-material-design: true
